server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/byte-bites-restaurant
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: testLatticeApp

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true